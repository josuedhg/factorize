#ifndef FACTORIAL_H
#define FACTORIAL_H

#include <stdint.h>
/**
* @brief fuction to return the list of factors of a number
*
* @param[in] Number The number to be used to get the factors
* @param[out] Pointer Pointer to a pointer to return the list of factors
* @return the size of the factors list
*/
uint16_t factors(uint16_t, uint16_t**);

#endif
